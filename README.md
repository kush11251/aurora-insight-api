# Aurora Insight API
A real-time data analytics and visualization platform.
## Getting Started
1. Install dependencies with `npm install`.
2. Start the server with `node src/index.js`.
## API Endpoints
* `/metrics`: Returns current system metrics.
* `/visualization`: Returns a JSON object containing data for visualization.